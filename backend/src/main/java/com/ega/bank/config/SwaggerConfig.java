package com.ega.bank.config;

import org.springframework.context.annotation.Configuration;

@Configuration
// SpringDoc OpenAPI is auto-configured. Customization can be done here.
public class SwaggerConfig {
    // Basic configuration if needed, or stick to defaults.
}
